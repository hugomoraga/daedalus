// Fixture placeholder for Theia parser tests (BUG-001).
//
// `tools/theia/tests/fixtures/repo-typical/apps/atlas/` exists so that
// `parseCodeInventory` finds ≥2 apps in the fixture. The parser only
// walks top-level entries of `apps/` / `packages/` / `tests/`; the
// contents of these stub files are not consumed by any test. See
// `tools/theia/tests/parser-inventory-use-cases.test.ts` AC-6.

export {};