// @daedalus/proposal-generation/adapters — driven adapters, behind an explicit subpath
// so they are never pulled in as part of the module's normal public API.

export { JsonFileDraftStoreAdapter } from "./json-draft-store.ts";
